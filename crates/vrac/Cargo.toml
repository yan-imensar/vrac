[package]
name = "vrac"
version.workspace = true
edition.workspace = true
license.workspace = true

[[bin]]
name = "vrac"
path = "src/main.rs"
doc = false

[dependencies]
vrac = { package = "vrac-engine", path = "../vrac-engine", version = "0.1.0" }
vrac-tui = { path = "../vrac-tui", version = "0.1.0" }

[dev-dependencies]
libc = "0.2"
tempfile = "3.27.0"
